/*Easypiechart Init*/

$(document).ready(function() {
	"use strict";
	
	if( $('#pie_chart_1').length > 0 ){
		$('#pie_chart_1').easyPieChart({
			barColor : '#07978F',
			lineWidth: 6,
			animate: 3000,
			size:	100,
			lineCap: 'square',
			scaleColor: 'rgba(33,33,33,.1)',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	if( $('#pie_chart_2').length > 0 ){
		$('#pie_chart_2').easyPieChart({
			barColor : '#07978F',
			lineWidth: 6,
			animate: 3000,
			size:	100,
			lineCap: 'square',
			scaleColor: 'rgba(33,33,33,.1)',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	if( $('#pie_chart_3').length > 0 ){
		$('#pie_chart_3').easyPieChart({
			animate: 3000,
			barColor : '#07978F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}	
	if( $('#pie_chart_4').length > 0 ){
		$('#pie_chart_4').easyPieChart({
			animate: 3000,
			barColor : '#07978F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	if( $('#pie_chart_5').length > 0 ){
		$('#pie_chart_5').easyPieChart({
			animate: 3000,
			barColor : '#07978F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
    if( $('#pie_chart_6').length > 0 ){	
		$('#pie_chart_6').easyPieChart({
			animate: 3000,
			barColor : '#07978F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	if( $('#pie_chart_7').length > 0 ){
		$('#pie_chart_7').easyPieChart({
			animate: 3000,
			barColor : '#07978F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	
	if( $('#pie_chart_8').length > 0 ){
		$('#pie_chart_8').easyPieChart({
			animate: 3000,
			barColor : '#D3B32F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_9').length > 0 ){
		$('#pie_chart_9').easyPieChart({
			animate: 3000,
			barColor : '#D3B32F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_10').length > 0 ){
		$('#pie_chart_10').easyPieChart({
			animate: 3000,
			barColor : '#D3B32F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_11').length > 0 ){
		$('#pie_chart_11').easyPieChart({
			animate: 3000,
			barColor : '#D3B32F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_12').length > 0 ){
		$('#pie_chart_12').easyPieChart({
			animate: 3000,
			barColor : '#D3B32F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_13').length > 0 ){
		$('#pie_chart_13').easyPieChart({
			animate: 3000,
			barColor : '#D3B32F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_14').length > 0 ){
		$('#pie_chart_14').easyPieChart({
			animate: 3000,
			barColor : '#D3B32F',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	
	if( $('#pie_chart_15').length > 0 ){
		$('#pie_chart_15').easyPieChart({
			animate: 3000,
			barColor : '#D26A07',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_16').length > 0 ){
		$('#pie_chart_16').easyPieChart({
			animate: 3000,
			barColor : '#D26A07',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_17').length > 0 ){
		$('#pie_chart_17').easyPieChart({
			animate: 3000,
			barColor : '#D26A07',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_18').length > 0 ){
		$('#pie_chart_18').easyPieChart({
			animate: 3000,
			barColor : '#D26A07',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_19').length > 0 ){
		$('#pie_chart_19').easyPieChart({
			animate: 3000,
			barColor : '#D26A07',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_20').length > 0 ){
		$('#pie_chart_20').easyPieChart({
			animate: 3000,
			barColor : '#D26A07',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_21').length > 0 ){
		$('#pie_chart_21').easyPieChart({
			animate: 3000,
			barColor : '#D26A07',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_22').length > 0 ){
		$('#pie_chart_22').easyPieChart({
			animate: 3000,
			barColor : '#085B8A',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_23').length > 0 ){
		$('#pie_chart_23').easyPieChart({
			animate: 3000,
			barColor : '#085B8A',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_24').length > 0 ){
		$('#pie_chart_24').easyPieChart({
			animate: 3000,
			barColor : '#085B8A',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_25').length > 0 ){
		$('#pie_chart_25').easyPieChart({
			animate: 3000,
			barColor : '#085B8A',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_26').length > 0 ){
		$('#pie_chart_26').easyPieChart({
			animate: 3000,
			barColor : '#085B8A',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_27').length > 0 ){
		$('#pie_chart_27').easyPieChart({
			animate: 3000,
			barColor : '#085B8A',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_28').length > 0 ){
		$('#pie_chart_28').easyPieChart({
			animate: 3000,
			barColor : '#085B8A',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_29').length > 0 ){
		$('#pie_chart_29').easyPieChart({
			animate: 3000,
			barColor : '#D32B4B',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_30').length > 0 ){
		$('#pie_chart_30').easyPieChart({
			animate: 3000,
			barColor : '#D32B4B',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_31').length > 0 ){
		$('#pie_chart_31').easyPieChart({
			animate: 3000,
			barColor : '#D32B4B',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_32').length > 0 ){
		$('#pie_chart_32').easyPieChart({
			animate: 3000,
			barColor : '#D32B4B',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_33').length > 0 ){
		$('#pie_chart_33').easyPieChart({
			animate: 3000,
			barColor : '#D32B4B',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_34').length > 0 ){
		$('#pie_chart_34').easyPieChart({
			animate: 3000,
			barColor : '#D32B4B',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_35').length > 0 ){
		$('#pie_chart_35').easyPieChart({
			animate: 3000,
			barColor : '#D32B4B',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_36').length > 0 ){
		$('#pie_chart_36').easyPieChart({
			animate: 3000,
			barColor : '#43B457',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	if( $('#pie_chart_37').length > 0 ){
		$('#pie_chart_37').easyPieChart({
			animate: 3000,
			barColor : '#43B457',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_38').length > 0 ){
		$('#pie_chart_38').easyPieChart({
			animate: 3000,
			barColor : '#43B457',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_39').length > 0 ){
		$('#pie_chart_39').easyPieChart({
			animate: 3000,
			barColor : '#43B457',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_40').length > 0 ){
		$('#pie_chart_40').easyPieChart({
			animate: 3000,
			barColor : '#43B457',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_41').length > 0 ){
		$('#pie_chart_41').easyPieChart({
			animate: 3000,
			barColor : '#43B457',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_42').length > 0 ){
		$('#pie_chart_42').easyPieChart({
			animate: 3000,
			barColor : '#43B457',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
	if( $('#pie_chart_43').length > 0 ){
		$('#pie_chart_43').easyPieChart({
			animate: 3000,
			barColor : '#D3362D',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_44').length > 0 ){
		$('#pie_chart_44').easyPieChart({
			animate: 3000,
			barColor : '#D3362D',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_45').length > 0 ){
		$('#pie_chart_45').easyPieChart({
			animate: 3000,
			barColor : '#D3362D',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_46').length > 0 ){
		$('#pie_chart_46').easyPieChart({
			animate: 3000,
			barColor : '#D3362D',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_47').length > 0 ){
		$('#pie_chart_47').easyPieChart({
			animate: 3000,
			barColor : '#D3362D',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_48').length > 0 ){
		$('#pie_chart_48').easyPieChart({
			animate: 3000,
			barColor : '#D3362D',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	if( $('#pie_chart_49').length > 0 ){
		$('#pie_chart_49').easyPieChart({
			animate: 3000,
			barColor : '#D3362D',
			lineWidth: 6,
			scaleColor: 'rgba(33,33,33,.1)',
			size:	100,
			lineCap: 'square',
			trackColor: '#fff',
			onStep: function(from, to, percent) {
				$(this.el).find('.percent').text(Math.round(percent));
			}
		});
	}
	
	
});